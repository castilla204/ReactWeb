/** Tokens mínimos para hero placeholder (gradiente + líneas). */
export const HP_SK = {
  fill: 'hsl(var(--surface-tinted))',
  line: 'hsl(var(--line))',
  inkGhost: 'hsl(var(--line))',
  mutedLine: 'hsl(var(--line-soft))',
  heroGradient:
    'linear-gradient(165deg, hsl(var(--surface-tinted)) 0%, hsl(var(--surface)) 52%, hsl(var(--surface-tinted)) 100%)',
} as const;
