import React from 'react';

const Background: React.FC = () => (
    <div className="absolute inset-0 z-0 overflow-hidden">
        {/* Base background - simple and clean */}
        <div className="absolute inset-0 bg-background" />
    </div>
);

export default Background;