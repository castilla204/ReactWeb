/** Línea de confianza escrow — homepage y componentes de confianza. */
export const ESCROW_TRUST_TAGLINE = 'No pagamos al experto hasta que tú confirmes';

/** Primera línea del hero móvil — entregable + precio. */
export const ESCROW_HERO_MOBILE_LEAD = 'Informe con fotos y vídeo · Precio fijo, sin sorpresas';

/** Chip / pill — titular corto (cabecera del chupachups). */
export const ESCROW_TRUST_CHIP_HEADLINE = 'Compra protegida';

/** Pill hero móvil — segunda línea del chupachups (teaser). */
export const ESCROW_TRUST_PILL_SUBLINE = 'Pagas al recibir el informe';

/** Panel de confianza — titular (mecanismo en lenguaje claro). */
export const ESCROW_TRUST_PANEL_HEADLINE =
  'El pago no llega al experto hasta que tú lo apruebes';

/** Panel de confianza — qué recibes y cuándo se mueve el dinero. */
export const ESCROW_TRUST_PANEL_BODY =
  'Primero recibes el informe con fotos y vídeo. Después se libera el pago.';

/** Panel de confianza — credibilidad y política. */
export const ESCROW_TRUST_PANEL_PROOF =
  'Peritos verificados · Cancelación gratuita antes del servicio';

/** Panel de confianza — enlace a ayuda. */
export const ESCROW_TRUST_PANEL_CTA = 'Cómo funciona el pago seguro';

/** Panel desktop — titular (mensaje de la ola de confianza). */
export const ESCROW_TRUST_DESKTOP_PANEL_HEADLINE = 'Pago retenido';

/** Panel desktop — detalle bajo el titular. */
export const ESCROW_TRUST_DESKTOP_PANEL_BODY =
  'Peritos verificados e informe con fotos y vídeo antes de liberar el pago.';

/** @deprecated Usar ESCROW_TRUST_PILL_SUBLINE (pill) y ESCROW_TRUST_PANEL_HEADLINE (panel). */
export const ESCROW_HERO_MOBILE_TAGLINE = 'No pagas al experto hasta confirmar';

/** Nota única sobre el CTA en checkout móvil — sin lista ni iconos. */
export const ESCROW_CHECKOUT_FOOTER_NOTE =
  'Tras reservar coordinas fecha y lugar por chat. Cancelación gratuita antes del inicio.';

/** Recordatorio operativo en checkout desktop (columna izquierda). */
export const ESCROW_CHECKOUT_COORDINATION_NOTE =
  'Coordinas fecha y lugar por chat (mín. 24 h de antelación).';

/** Punto de confianza — cancelación en checkout móvil. */
export const ESCROW_CHECKOUT_CANCELLATION_LINE =
  'Reembolso completo si cancelas antes de que empiece el servicio.';

/** Punto de confianza — coordinación en checkout móvil. */
export const ESCROW_CHECKOUT_COORDINATION_LINE = ESCROW_CHECKOUT_COORDINATION_NOTE;

/** @deprecated Usar ESCROW_CHECKOUT_CANCELLATION_LINE */
export const ESCROW_CHECKOUT_CANCELLATION_NOTE = ESCROW_CHECKOUT_CANCELLATION_LINE;
